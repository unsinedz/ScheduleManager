@model DateTime?
@{
    var inputValue = Model.HasValue
        ? Model.Value.ToString("t")
        : null;
}
@Html.TextBox("", inputValue, new { @class = "text-box single-line timepicker", placeholder = Html.DisplayName("") })